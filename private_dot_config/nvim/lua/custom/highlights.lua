local colors = require('custom.themes.oceanic-next').base_30

return {
  St_gitIcons = {
    fg = colors.black,
    bg = colors.pink,
  },
  St_pos_text = {
    fg = colors.black,
    bg = colors.green,
  },
}
